services:
  jellyfin:
    labels:
      # Websecure
      # Use the chain-Domain-auth middleware for domain handling
      traefik.http.routers.jellyfin-rodrigomescua.middlewares: chain-Domain-auth@file
      # Use the default certresolver
      traefik.http.routers.jellyfin-rodrigomescua.tls.certresolver: ''
      # WebSecure local IP
      # Use additional router for local access
      traefik.http.routers.jellyfin-rodrigomescua-privip.priority: 100
      traefik.http.routers.jellyfin-rodrigomescua-privip.rule: Host(`${APP_DOMAIN}`) && ClientIP(`${PRIVATE_IPV4}`)
      traefik.http.routers.jellyfin-rodrigomescua-privip.middlewares: chain-Domain@file
      traefik.http.routers.jellyfin-rodrigomescua-privip.entrypoints: websecure
      traefik.http.routers.jellyfin-rodrigomescua-privip.service: jellyfin-rodrigomescua
      traefik.http.routers.jellyfin-rodrigomescua-privip.tls.certresolver: ''

