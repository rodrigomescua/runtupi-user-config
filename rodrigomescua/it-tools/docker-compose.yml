services:
  it-tools:
    labels:
      # Websecure
      # Use the chain-public-auth middleware for domain handling
      traefik.http.routers.it-tools-rodrigomescua.middlewares: chain-public-auth@file
      # Use the default certresolver
      traefik.http.routers.it-tools-rodrigomescua.tls.certresolver: ''
      # WebSecure local IP
      # Use additional router for local access
      traefik.http.routers.it-tools-rodrigomescua-privip.rule: Host(`${APP_DOMAIN}`) && ClientIP(`${PRIVATE_IPV4}`)
      traefik.http.routers.it-tools-rodrigomescua-privip.middlewares: chain-public@file
      traefik.http.routers.it-tools-rodrigomescua-privip.entrypoints: websecure
      traefik.http.routers.it-tools-rodrigomescua-privip.service: it-tools-rodrigomescua
      traefik.http.routers.it-tools-rodrigomescua-privip.priority: 100
      traefik.http.routers.it-tools-rodrigomescua-privip.tls.certresolver: ''


