services:
  homeassistant:
    labels:
      # Websecure
      # Use the chain-Domain-auth middleware for domain handling
      traefik.http.routers.homeassistant-rodrigomescua.middlewares: chain-Domain-auth@file
      # Use the default certresolver
      traefik.http.routers.homeassistant-rodrigomescua.tls.certresolver: ''
      # WebSecure local IP
      # Use additional router for local access
      traefik.http.routers.homeassistant-rodrigomescua-privip.rule: Host(`${APP_DOMAIN}`) && ClientIP(`${PRIVATE_IPV4}`)
      traefik.http.routers.homeassistant-rodrigomescua-privip.middlewares: chain-Domain@file
      traefik.http.routers.homeassistant-rodrigomescua-privip.entrypoints: websecure
      traefik.http.routers.homeassistant-rodrigomescua-privip.service: homeassistant-rodrigomescua
      traefik.http.routers.homeassistant-rodrigomescua-privip.priority: 100
      traefik.http.routers.homeassistant-rodrigomescua-privip.tls.certresolver: ''

