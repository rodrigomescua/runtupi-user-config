services:
  ddns-updater:
    labels:
      # Websecure
      # Use the chain-public-auth middleware for domain handling
      traefik.http.routers.ddns-updater-cf-rodrigomescua.middlewares: chain-public-auth@file
      # Use the default certresolver
      traefik.http.routers.ddns-updater-cf-rodrigomescua.tls.certresolver: ''


