services:
  moodist:
    labels:
      # Websecure
      # Use the chain-Domain-auth middleware for domain handling
      traefik.http.routers.moodist-rodrigomescua.middlewares: chain-Domain-auth@file
      # Use the default certresolver
      traefik.http.routers.moodist-rodrigomescua.tls.certresolver: ''
      # WebSecure local IP
      # Use additional router for local access
      traefik.http.routers.moodist-rodrigomescua-privip.rule: Host(`${APP_DOMAIN}`) && ClientIP(`${PRIVATE_IPV4}`)
      traefik.http.routers.moodist-rodrigomescua-privip.middlewares: chain-Domain@file
      traefik.http.routers.moodist-rodrigomescua-privip.entrypoints: websecure
      traefik.http.routers.moodist-rodrigomescua-privip.service: moodist-rodrigomescua
      traefik.http.routers.moodist-rodrigomescua-privip.priority: 100
      traefik.http.routers.moodist-rodrigomescua-privip.tls.certresolver: ''

